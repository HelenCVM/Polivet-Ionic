import { FiltroByIdPipe } from './filtro-by-id.pipe';

describe('FiltroByIdPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroByIdPipe();
    expect(pipe).toBeTruthy();
  });
});
