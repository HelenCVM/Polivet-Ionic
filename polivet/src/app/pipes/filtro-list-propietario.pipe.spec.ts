import { FiltroListPropietarioPipe } from './filtro-list-propietario.pipe';

describe('FiltroListPropietarioPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroListPropietarioPipe();
    expect(pipe).toBeTruthy();
  });
});
